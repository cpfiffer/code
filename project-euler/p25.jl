function fib(fibdict, num)
    if num == 1 || num == 2
        return 1
    else
        num1 = get(fiblist, num - 1, -1)
        num2 = get(fiblist, num - 2, -1)

        if num1
        if in(fiblist)
    end

end
